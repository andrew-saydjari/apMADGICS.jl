module apMADGICS

    using LowRankOps, EllipsisNotation, ShiftedArrays, FITSIO, HDF5

    # At some point, we should consider making this a real module, not just a repo of function definitions
    # I should at the very least harmonizing these usings with the pipeline script so that people can use the enviornment in the repo

end
